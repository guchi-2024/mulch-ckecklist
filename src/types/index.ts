export interface ListItems {
  id: string,
  ship: string,
  area: string,
  position: string,
  name: string,
  number: number,
  startDate: string,
  endDate: string
}

export interface MenuItem {
  text: string,
  path: string,
  areaBtn: string[]
}

